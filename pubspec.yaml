name: test_application  # 替换为你的项目名
description: A new Flutter project.

publish_to: 'none' # 如果你不发布到pub.dev就保持none

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # - assets/images/
